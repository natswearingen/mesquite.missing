python3 script for finding the percentage of missing data in a Mesquite matrix 

NOTE  this script cannot read the character state names block when you save a matrix as a nexus file from mesquite. manually delete it in a text 
editor so the file look sliek the example.nex 
